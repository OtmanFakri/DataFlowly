# Paddle Configuration
VITE_PADDLE_ENVIRONMENT=sandbox
VITE_PADDLE_TOKEN_CLIENT=your-client-token-here

# Note: Client Token is used for modern client-side Paddle integration
# Get this from your Paddle dashboard under Developer Tools > Authentication
# Make sure to install @paddle/paddle-js package: npm install @paddle/paddle-js 